@model IEnumerable<BUS.Models.Route>

@{
    ViewBag.Title = "Route Management";
    Layout = "~/Views/Shared/Admin.cshtml";
}

<div class="container mt-4">
    <h2 class="text-center mb-4">Route Management</h2>

    <p class="text-center">
        @Html.ActionLink("Create New Route", "Create", null, new { @class = "btn btn-primary" })
    </p>

    <div class="table-responsive">
        <table class="table table-striped table-bordered">
            <thead class="thead-dark">
                <tr>
                    <th>@Html.DisplayNameFor(model => model.First().START_LOCATION)</th>
                    <th>@Html.DisplayNameFor(model => model.First().END_LOCATION)</th>
                    <th>@Html.DisplayNameFor(model => model.First().DISTANCE)</th>
                    <th class="text-center">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => item.START_LOCATION)</td>
                        <td>@Html.DisplayFor(modelItem => item.END_LOCATION)</td>
                        <td>@Html.DisplayFor(modelItem => item.DISTANCE)</td>
                        <td class="text-center">
                            @Html.ActionLink("Edit", "Edit", new { id = item.ROUTE_ID }, new { @class = "btn btn-warning btn-sm" }) |
                           
                            @Html.ActionLink("Delete", "Delete", new { id = item.ROUTE_ID }, new { @class = "btn btn-danger btn-sm" })
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<style>

    h2 {
        color: #333;
    }

    .table {
        margin-top: 20px;
        font-size: 16px;
    }

    .thead-dark {
        background-color: #343a40;
        color: white;
    }

    .btn-primary {
        margin-bottom: 15px;
    }

    .btn-sm {
        padding: 5px 10px; /* Smaller button padding */
    }

    .text-center {
        text-align: center;
    }

    .table-responsive {
        overflow-x: auto; /* Enables horizontal scrolling for smaller screens */
    }
</style>
